const VOUCHER_API="https://ca-smart-staycation-muqd.onrender.com/api";let appliedVoucher=null;let voucherReady=false;function voucherMoney(v){return"₱"+Number(v||0).toLocaleString("en-PH")}function voucherNights(){const a=document.getElementById("checkIn")?.value,b=document.getElementById("checkOut")?.value;return a&&b?Math.max(0,Math.ceil((new Date(b)-new Date(a))/86400000)):0}function voucherEligibleNights(){const nights=voucherNights();const limit=Number(appliedVoucher?.maxNights||0);return limit>0?Math.min(nights,limit):nights}function voucherRoomNightRate(){const nights=voucherNights();const room=Number(document.getElementById("roomAmount")?.innerText.replace("₱","").replace(/,/g,"")||0);return nights>0?room/nights:0}async function validateGuestVoucher(showMessage=true){const input=document.getElementById("voucherCode"),status=document.getElementById("voucherStatus"),code=String(input?.value||"").trim().toUpperCase();if(!code){appliedVoucher=null;voucherReady=false;if(status)status.textContent="";calculateTotal();return false}try{if(status)status.textContent="Checking voucher...";const checkIn=document.getElementById("checkIn")?.value||"",checkOut=document.getElementById("checkOut")?.value||"",res=await fetch(`${VOUCHER_API}/vouchers/validate`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({code,checkIn,checkOut})}),json=await res.json();if(!res.ok)throw Error(json.message||"Invalid voucher.");appliedVoucher=json.data;voucherReady=true;if(status)status.textContent=`${appliedVoucher.code} applied — ${appliedVoucher.discountPercent}% discount${appliedVoucher.maxNights?` · Maximum ${appliedVoucher.maxNights} night${appliedVoucher.maxNights===1?"":"s"}`:""}.`;calculateTotal();return true}catch(err){appliedVoucher=null;voucherReady=false;if(status)status.textContent=err.message||"Invalid voucher.";calculateTotal();if(showMessage)alert(err.message||"Invalid voucher.");return false}}function calculateVoucherDiscount(roomAmount,extraAmount,parkingAmount){const rate=voucherRoomNightRate(),eligible=voucherEligibleNights(),pct=Number(appliedVoucher?.discountPercent||0)/100;const eligibleRoom=rate*eligible;const otherEligible=0;return appliedVoucher&&voucherReady?Math.min(Number(roomAmount||0)+Number(extraAmount||0)+Number(parkingAmount||0),eligibleRoom*pct+otherEligible):0}function renderVoucherSummary(roomAmount,extraAmount,parkingAmount,depositAmount,originalTotal){const box=document.getElementById("voucherSummary"),discountRow=document.getElementById("voucherDiscountRow"),discountAmount=document.getElementById("voucherDiscountAmount");if(!box||!discountRow||!discountAmount)return;const base=Number(roomAmount||0)+Number(extraAmount||0)+Number(parkingAmount||0),discount=calculateVoucherDiscount(roomAmount,extraAmount,parkingAmount),finalTotal=base-discount+Number(depositAmount||0);discountRow.style.display=discount>0?"flex":"none";discountAmount.textContent=`-${voucherMoney(discount)}`;if(document.getElementById("totalAmount"))document.getElementById("totalAmount").innerText=voucherMoney(finalTotal);box.dataset.discount=String(discount);return{discount,finalTotal,subtotal:base}}function installVoucherSummaryHook(){if(typeof window.updateSummary!=="function"||window.updateSummary.__voucherWrapped)return;const original=window.updateSummary;function wrapped(roomAmount,extraAmount,parkingAmount,depositAmount,total){original(roomAmount,extraAmount,parkingAmount,depositAmount,total);renderVoucherSummary(roomAmount,extraAmount,parkingAmount,depositAmount,total)}wrapped.__voucherWrapped=true;window.updateSummary=wrapped}document.addEventListener("DOMContentLoaded",()=>{installVoucherSummaryHook();const apply=document.getElementById("applyVoucher");if(apply)apply.addEventListener("click",()=>validateGuestVoucher(true));const input=document.getElementById("voucherCode");if(input)input.addEventListener("input",()=>{appliedVoucher=null;voucherReady=false;const s=document.getElementById("voucherStatus");if(s)s.textContent="";calculateTotal()});["checkIn","checkOut"].forEach(id=>document.getElementById(id)?.addEventListener("change",()=>{if(String(input?.value||"").trim())validateGuestVoucher(true)}));const form=document.getElementById("guestBookingForm");if(form){let bypass=false;form.addEventListener("submit",async event=>{if(bypass){bypass=false;return}const code=String(input?.value||"").trim().toUpperCase();if(!code||(appliedVoucher&&voucherReady&&appliedVoucher.code===code))return;event.preventDefault();event.stopImmediatePropagation();const valid=await validateGuestVoucher(true);if(valid){bypass=true;form.requestSubmit()}},true)}});const originalGuestFetch=window.fetch.bind(window);window.fetch=async function(input,init={}){try{const url=typeof input==="string"?input:input.url;if(String(url).endsWith("/api/bookings")&&String(init.method||"GET").toUpperCase()==="POST"&&init.body&&appliedVoucher&&voucherReady){const body=JSON.parse(init.body),roomAmount=Number(document.getElementById("roomAmount")?.innerText.replace("₱","").replace(/,/g,"")||0),extraAmount=Number(document.getElementById("extraGuestAmount")?.innerText.replace("₱","").replace(/,/g,"")||0),parkingAmount=Number(document.getElementById("parkingAmount")?.innerText.replace("₱","").replace(/,/g,"")||0),depositAmount=Number(document.getElementById("securityDepositAmount")?.innerText.replace("₱","").replace(/,/g,"")||0),base=roomAmount+extraAmount+parkingAmount,discount=calculateVoucherDiscount(roomAmount,extraAmount,parkingAmount);body.subtotalAmount=base;body.voucherCode=appliedVoucher.code;body.voucherDiscountPercent=Number(appliedVoucher.discountPercent||0);body.voucherDiscountAmount=discount;body.voucherMaxNights=appliedVoucher.maxNights||null;body.voucherDiscountedNights=voucherEligibleNights();body.totalAmount=base-discount+depositAmount;init={...init,body:JSON.stringify(body)}}}catch(err){console.error("Voucher booking preparation error:",err)}return originalGuestFetch(input,init)};